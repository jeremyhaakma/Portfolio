#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void){
    double s1, s2, s3;
    printf("please enter the first judge's score");
    fflush(stdout);
    scanf("%lg", &s1);
    return EXIT_SUCCESS;
}
